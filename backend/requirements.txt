# Web framework
fastapi==0.138.0
# starlette is pinned explicitly (transitive via fastapi) to guarantee the CVE-clean
# release: GHSA-82w8 (form limits), GHSA-86qp (Host poisoning), GHSA-wqp7 (StaticFiles SSRF) et al.
starlette==1.3.1
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Auth
itsdangerous==2.2.0
httpx==0.28.1
PyJWT[crypto]>=2.13.0
bcrypt==4.2.1
cryptography==49.0.0

# Rate limiting
slowapi==0.1.9

# Cache / Queue
redis==5.2.1

# Image processing / RTSP
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==11.0.0

# Config / validation
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
python-multipart==0.0.32

# Retry / resilience
tenacity==9.0.0

# Utilities
aiofiles==24.1.0
aiosignal==1.3.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
respx==0.21.1
aiosqlite==0.20.0
PyYAML==6.0.2  # tests/test_deploy_artifacts.py parses compose + CI YAML
